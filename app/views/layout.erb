<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>MakersBnb</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="/css/style.css" rel="stylesheet">
  <link href="/css/normalize.css" rel="stylesheet">
  <!-- <link href="/css/bootstrap.css" rel="stylesheet"> -->
  <link href="http://netdna.bootstrapcdn.com/twitter-bootstrap/2.3.2/css/bootstrap-combined.min.css" rel="stylesheet">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</head>
<body>
  <div class="navbar">
    <div class="navbar-inner">
      <div class="container-fluid">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="/listings" name="top"> MakersBnb </a>
        <div class="nav-collapse collapse">
          <ul class="nav navbar-nav navbar-left">
            <li><a href="/listings"><i class="icon-home"></i> Spaces </a></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if current_user %>
            <li><h4 style="color:black;display:inline-block;">Welcome <%= current_user.first_name %></h4></li>
            <li>
              <a href="/sessions/user/spaces">My Spaces</a>
            </li>
            <li>
              <a href="/space/add">Add New Space</a>
            </li>
            <li>
              <form method='post' action='/sessions' class="navbar-form navbar-right">
                <input type='hidden' name='_method' value='delete'>
                <button type="submit" class="btn btn-danger">Log Out</button>
              </form>
            </li>
          </ul>
            <% else %>
            <ul class="nav pull-right">
              <li>
                <a href="/user/new"><i class="icon-user"></i> New User</a>
              </li>
              <li>
                <form method='post' action='/sessions/new' class="navbar-form navbar-right">
                  <input type='hidden' name='_method' value='get'>
                  <button type="submit" class="btn btn-primary" id = "login">Log In</button>
                </form>
              </li>
            </ul>
            <% end %>
          </div>
          <!--/.nav-collapse -->
        </div>
        <!--/.container-fluid -->
      </div>
      <!--/.navbar-inner -->



  <% if flash[:notice] %>
    <div id='notice'><%= flash[:notice] %></div>
  <% end %>

  <div class="errors">
    <% if flash[:errors] && !flash[:errors].empty? %>
    <div class="alert alert-warning" role="alert">
      <a href="#" class="close" data-dismiss="alert">&times;</a>
      <ul id = 'errors'>
        <% flash[:errors].each do |error| %>
        <li><%= error %></li>
        <% end %>
      </ul>
    </div>
    <% end %>
  </div>
</div>
<!--/.navbar -->
</div>
  <%= yield %>
</body>
</html>
